package com.kelseyde.calvin.evaluation;

import com.kelseyde.calvin.board.Game;

public class SimplePositionEvaluator implements PositionEvaluator {
    @Override
    public int evaluate(Game game) {
        return 0;
    }
}
