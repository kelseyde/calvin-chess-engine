spring:
  application:
    name: calvin
  main:
    banner-mode: off

http:
  enabled: true

server:
  port: 8080
  servlet:
    context-path: /${spring.application.name}

logging:
  file:
    name: calvin_debug.log
    max-size: 1
    clean-history-on-start: true
  level:
    root: WARN
    com.kelseyde.calvin: DEBUG