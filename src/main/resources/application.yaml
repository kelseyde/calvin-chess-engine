spring:
  application:
    name: calvin
  main:
    lazy-initialization: true
    banner-mode: off

http:
  enabled: true

server:
  port: 8082
  servlet:
    context-path: /${spring.application.name}

logging:
  level:
    com.kelseyde.calvin: INFO